

targetCustomizations:
- name: qa
  kustomize:
    dir: ./envs/qa
  clusterSelector:
    matchLabels:
      env: dev

- name: staging
  kustomize:
    dir: ./envs/staging
  clusterSelector:
    matchLabels:
      env: dev
  # clusterName: dev-cluster